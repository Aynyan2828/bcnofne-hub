# bcnofne-hub 環境変数サンプル
# このサイトは公開静的物のため、秘密情報（APIキー等）は一切置かないこと。
# 通常は設定不要（astro.config.mjs の既定＝bcnofne.com / ルート で動く）。
# 以下はデプロイ先を切り替えたい時だけ使う任意の上書き値。

# 公開URLのオリジン（本番は独自ドメイン）
SITE_URL=https://bcnofne.com

# サブパス（独自ドメインなら /、GitHub PagesプロジェクトURLで確認したいなら /bcnofne-hub）
BASE_PATH=/
